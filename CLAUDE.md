# CLAUDE.md

이 파일은 로컬 작업용 짧은 라우터다.

실제 프로젝트 규칙은 루트 `docs/` 아래에 있다.

작업 규칙:

- 먼저 `docs/README.md`를 읽는다.
- 세부 규칙은 `docs/rules/`에서 작업 성격에 맞게 선택한다.
- 이 파일에는 요약만 두고 세부 규칙은 중복하지 않는다.

읽기 경로:

1. `docs/README.md`
2. 작업별 세부 문서
