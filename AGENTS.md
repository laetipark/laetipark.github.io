# AGENTS.md

이 파일은 짧은 작업 라우터다.

작업 전 먼저 `docs/README.md`를 읽고, 그 문서가 연결하는 작업별 규칙을 따른다.

핵심 원칙:

- 실제 규칙 정본은 `docs/`에 둔다.
- 이 파일에는 세부 규칙을 중복 작성하지 않는다.
- 구현 전에 작업 성격에 맞는 문서만 선택해서 읽는다.

최소 읽기 순서:

1. `docs/README.md`
2. 관련 세부 문서
